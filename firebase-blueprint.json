{
  "entities": {
    "mediaKit": {
      "title": "Media Kit Content",
      "description": "Configurações e conteúdos completos do mídia kit",
      "type": "object",
      "properties": {
        "creator": { "type": "object" },
        "metrics": { "type": "object" },
        "instagram": { "type": "object" },
        "tiktok": { "type": "object" },
        "segments": { "type": "array" },
        "brands": { "type": "array" },
        "photos": { "type": "object" },
        "contact": { "type": "object" }
      }
    },
    "leads": {
      "title": "Propostas Comerciais / Leads",
      "description": "Solicitações de contato enviadas por marcas através do formulário",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "brand": { "type": "string" },
        "budget": { "type": "string" },
        "message": { "type": "string" },
        "createdAt": { "type": "string" }
      }
    },
    "adminUsers": {
      "title": "Usuários Administradores",
      "description": "Registro de permissão para administradores",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "role": { "type": "string" },
        "createdAt": { "type": "string" }
      }
    }
  },
  "firestoreRules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    function isAuthenticated() {\n      return request.auth != null;\n    }\n    function isAdmin() {\n      return isAuthenticated() && (\n        request.auth.token.email == 'sophiaamenezes10@gmail.com' ||\n        exists(/databases/$(database)/documents/adminUsers/$(request.auth.uid))\n      );\n    }\n    match /content/mediaKit {\n      allow read: if true;\n      allow write: if isAdmin();\n    }\n    match /leads/{leadId} {\n      allow create: if request.resource.data.email is string \n                    && request.resource.data.name is string\n                    && request.resource.data.email.size() >= 5\n                    && request.resource.data.email.size() <= 120\n                    && request.resource.data.name.size() >= 2\n                    && request.resource.data.name.size() <= 100;\n      allow read, update, delete: if isAdmin();\n    }\n    match /adminUsers/{userId} {\n      allow read: if isAuthenticated();\n      allow write: if isAdmin();\n    }\n    match /{document=**} {\n      allow read, write: if false;\n    }\n  }\n}"
}
